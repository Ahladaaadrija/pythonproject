def is_leap_year(year):
    if year%4 == 0 and year%100 != 0 or year%400 == 0:
        return True
    else:
        return False
user = int(input("Enter the year?"))
y = is_leap_year(user)
if y == True:
    print(f"{user} is a leap year")
else:
    print(f"{user} is not a leapyear")
  
